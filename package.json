{
  "name": "task-tracker-web-client",
  "version": "1.0.0",
  "private": true,
  "description": "Web client for the Employee Task Management API. Ships as plain ES modules with no build step; this manifest only marks the sources as ESM and runs the test suite.",
  "type": "module",
  "scripts": {
    "test": "node --test \"frontend/tests/*.test.js\""
  }
}
